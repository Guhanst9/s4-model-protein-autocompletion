# dataset configuration for protein sequences
_target_: src.dataloaders.protein.ProteinDataset
fasta_file: "data/protein/train.fasta" 
l_max: 1024  # max sequence length
cache_dir: null  # directory for caching processed data
n_workers: 4  # dataloader workers